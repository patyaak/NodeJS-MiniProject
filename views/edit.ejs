<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div
      class="w-full min-h-screen flex flex-col items-center text-center p-10"
    >
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 px-3 py-2 rounded-md mb-5 inline-block text-white hover:bg-red-600 transition"
          href="/logout"
          >Logout</a
        >
      </div>
      <!-- Centered H1 in its own div -->
      <div class="w-full text-center">
        <h1 class="text-5xl font-bold mb-5">Hello, <%= user.name %>👋🏼</h1>
      </div>

      <!-- Separate div for H3 -->
      <div class="mb-5">
        <h3 class="text-lg">Edit Your Post</h3>
      </div>
      <form
        method="post"
        action="/update/<%= post._id %>"
        class="flex flex-col items-center"
      >
        <textarea
          placeholder="What's on your mind?"
          class="p-3 outline-none w-96 h-32 resize-none bg-transparent border-2 border-zinc-900 rounded-md"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="px-4 py-2 bg-yellow-500 text-white rounded-md mt-3 cursor-pointer hover:bg-yellow-600 transition"
          type="submit"
          value="Update your post"
        />
      </form>
    </div>
  </body>
</html>
